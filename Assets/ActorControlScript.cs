using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ActorControlScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
        int i = 0;
        
		//foreach(Transform child in transform)
        //{
          //  child.GetComponentInChildren<HeadRotate>().enabled = false;
            
        //}
        Debug.Log("the number of actors are: "+i);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
